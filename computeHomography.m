function H = computeHomography(Features, Matches, Model)
    % Features: Nx2 matrices storing the features [x, y]
    % Matches: Nx2 matrices storing the correspondences [x?, y?]
    % Model: string specifying a transformation model
    
    if Model == "Projective"
        
        
    end
    



end